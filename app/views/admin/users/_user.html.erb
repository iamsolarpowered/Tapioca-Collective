<% div_for user do -%>
  <%= image_tag h(user.gravatar_url), :alt => '' %>
  <div>
    <strong><%= link_to h(user.name), edit_admin_user_path(user) %></strong><br />
    <small>
      <strong><%=h user.title %></strong><br />
      <i><%=h user.location %></i><br />
      <%= simple_format user.blurb %>
    </small>
  </div>
  <br style="clear: left;" />
<% end -%>
